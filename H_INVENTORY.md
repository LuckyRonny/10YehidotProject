# רשימת קבצי הפרויקט (INVENTORY)

## סקירת פרויקט

מחברת וירטואלית שיתופית: מערכת לקוח–שרת מרובת משתמשים, התומכת בעריכה מקבילית של מחברת וירטואלית משותפת דרך רשת IP. השרת מנהל מחברות ונתוני משתמשים; הלקוחות יוצרים, עורכים ומשתפים מחברות. התקשורת מוצפנת ומבוססת על התחברות ואימות משתמשים. המחברות תומכות בבקרת גישה (צפייה/עריכה/ניהול). כל מחברת כוללת מספר עמודים עם טקסט, ציורים, קווי סימון, צבעים, רוחבי קו ורקעים שונים (ריק, שורות, משבצות). השרת שומר על שלמות המחברת ומאפשר עריכה סימולטנית; הלקוחות משתמשים בעותק מטמון לשיפור מהירות התגובה.

---

## קבצי Python

### צד השרת (`src/srvr/`)

| File | Description |
|------|-------------|
| **server.py** | נקודת הכניסה הראשית בצד השרת: יוצרת שקעי TCP (ערוץ ראשי וערוץ עדכונים), מקבלת לקוחות, מבצעת החלפת מפתחות לכל חיבור, ומריצה חוטי ביצוע לטיפול בכל לקוח. מנתבת בקשות אל `Methods` ושולחת תגובות; ערוץ העדכונים משרת רק בקשות `CHECK_UPDATES`. |
| **user_manager.py** | ניהול משתמשים ואימות: `LOGIN` (בדיקת שם משתמש/סיסמה מול SQLite והחזרת מזהה ושם תצוגה או כישלון),‏ `SIGNUP` (הוספת משתמש חדש והחזרת מזהה או כישלון אם יש כפילות),‏ `ALL_USERS` (רשימת משתמשים והרשאות למחברת מסוימת, למעט משתמש אחד). משתמש במסד `NotebookDB.db` ובסיסמה מגובבת ב־SHA‑256. |
| **user_notebook_manager.py** | קישור בין משתמשים למחברות והרשאותיהם: `ADD_NOTEBOOK_TO_DB` (יצירת מחברת במסד וב־`NotebookManager`),‏ `CLIENTS_NOTEBOOKS` (רשימת מחברות למשתמש עם ההרשאות),‏ `CHANGE_ACCESS` (שינוי או יצירת הרשאה למשתמש במחברת). נעזר ב־SQLite ומאציל את ניהול התוכן ל־`NotebookManager`. |
| **notebook_manager.py** | ניהול תוכן מחברות ועדכונים: טוען/שומר מחברות וקובצי עדכונים כ־JSON תחת `server_data/`. מטפל ב־`GET_NOTEBOOK`, `ADD_NOTEBOOK`, `ADD_STROKE`, `DELETE_STROKE`, `ADD_PAGE`, `CLEAR`, `CHANGE_BACKGROUND`, `CHECK_UPDATES`. משתמש במנעול לתמיכה בריבוי חוטים; שומר רשימת עדכונים ומקצר אותה כשנדרש. |
| **protocol.py** | פרוטוקול הודעות מבוסס אורך ורשות הצפנה: `send`/`recv` עבור מחרוזות ו־`send_bin`/`recv_bin` עבור בתים. מצפין/מפענח באמצעות AES כאשר מוגדר מפתח בחיבור. |
| **aes_cipher.py** | מימוש AES במצב CBC לערוץ המאובטח: ריווח/הסרת ריווח בסגנון PKCS7, הצפנה (וקטור אתחול + מוצפן מקודד Base64), פענוח ומתודת `generate_key` ליצירת מפתח מאוסף בתים אקראיים דרך SHA‑256. משתמש בחבילת PyCryptodome. |
| **constants.py** | קבועים בצד השרת: פורט, IP, אורך הודעה, אינדקסי פרמטרים בפרוטוקול/בקשות, אינדקסי עמודות במסד הנתונים ושמות עבור שדות מחברות, משתמשים וסוגי בקשות. |
| **diffie_hellman.py** | הסכמת מפתח ECDH על SECP384R1: יוצר זוג מפתחות, מסרלל/מפרש מפתח ציבורי בפורמט PEM וגוזר מפתח משותף באורך 32 בתים בעזרת HKDF‑SHA256. |
| **key_exchange.py** | שכבת החלפת מפתחות מעל הפרוטוקול: `send_recv_key` (שולח מפתח ציבורי מקומי, מקבל מפתח ציבורי מהצד השני וגוזר מפתח),‏ `recv_send_key` (מקבל ואז שולח, לשימוש בצד השרת). משתמש בשליחה/קבלה בינארית לא מוצפנת עבור שלב ה־DH. |
| **methods.py** | ניתוב בקשות: ממפה לפי תחום אל `UserManager` (USERS), `NotebookManager` (NOTEBOOKS) ו־`UserNotebookManager` (USERS_NOTEBOOKS) על סמך סוג הבקשה בפרמטרים. |
| **my_sha256.py** | עזרי SHA‑256: `get_hash` (גיבוב כבתים גולמיים) ו־`get_hash_hex` (ייצוג הקסה) לשימוש בסיסמאות וערכים נוספים. |

---

### צד הלקוח (`src/clnt/`)

| File | Description |
|------|-------------|
| **client.py** | רכיב הלקוח לניהול חיבורי TCP לשרת: קורא IP ופורט מרישום Windows, פותח שקעים לערוץ ראשי ולעדכונים, מבצע החלפת מפתחות לשניהם. מאמת את פורמט הבקשות ומספר הפרמטרים; המתודה `send_command` מנתבת `check_updates` לערוץ העדכונים ואת שאר הבקשות לערוץ הראשי. |
| **log_in.py** | חלון התחברות (PyQt6): שדות שם משתמש וסיסמה, כפתורי Login ו־Sign up ותווית שגיאה. בעת הצלחה שולח בקשת התחברות, פותח `MainWindow` עם אובייקט `Client` ומזהה/שם המשתמש, ומסתיר את חלון ההתחברות. |
| **sign_up.py** | חלון הרשמה: שדות שם משתמש, שם מלא וסיסמה, עם כפתור Sign up. שולח בקשת `signup`; במקרה הצלחה פותח `MainWindow` ונסגר, ובמקרה כישלון מציג הודעת שגיאה מתאימה. |
| **main_window.py** | חלון ראשי לאחר התחברות: סרגל כלים (שם משתמש, יציאה, רענון, הוספת מחברת), פריסת זרימה של כפתורי מחברות ותיבה צפה ליצירת מחברת חדשה. טוען מחברות דרך `clients_notebooks` ופותח `NotebookArea` בלחיצה על מחברת. |
| **notebook_area.py** | תצוגת מחברת יחידה: סרגלי כלים (page/pen/marker/eraser/select), אזור גלילה עם וידג'ט מחברת, כפתורי עמוד קודם/הבא/הוספה, ניקוי, שמירה וגישה. מריץ חוט רקע שמבצע `check_updates` ושולח אותות עדכון למחברת; מחיל עדכוני שרת (ADD_STROKE, DELETE_STROKE וכו') ומנהל דיאלוג הרשאות. |
| **notebook.py** | וידג'ט מחברת: עמודים כמחסנית (`QStackedWidget`) של `DrawingCanvas`, תמיכה בזום, הוספה ודפדוף בין עמודים. מסרלל למילון (`pages`, `last_change`) לצורך שמירה, ומיישם עדכונים מהשרת (ADD_PAGE, ADD_STROKE, DELETE_STROKE וכו') תוך שמירה על `last_change`. |
| **canvas.py** | קנבס ציור: תומך בכלי עט, מדגיש, מחק ובחירה; אוסף נקודות וזמנים למשיכות, מזהה קווים ישרים, תומך בזום (פקטור קנה מידה ו־pinch). רקעים: ריק, שורות, משבצות. מצייר משיכות, תומך בבחירה/הזזה ומחיקה, ומדווח ל־`notebook_area` על add_stroke/delete_stroke/clear_page/change_background. מממש את פעולות השרת ADD_STROKE/DELETE_STROKE/CLEAR/CHANGE_BACKGROUND על הקנבס. |
| **canvas_container.py** | וידג'ט מעטפת פשוט שמרכז ילד (בד"כ הקנבס) בפריסה אנכית/אופקית ומצייר רקע כחול בהיר מאחור; משמש לעיצוב סביב המחברת. |
| **scroll_area.py** | אזור גלילה ממורכז למחברת: Ctrl+גלגלת מפעיל זום פנימה/החוצה דרך מתודות הזום של המחברת; בשאר המקרים מתנהג כאזור גלילה רגיל. |
| **stroke.py** | מודל משיכת קו: רשימת נקודות (`QPoint` או זוגות (x,y)), רשימת זמנים, צבע ורוחב עט ומזהה. מספק `contains_point` לבדיקת פגיעה עם סף, ו־`__dict__` לסריאליזציה (נקודות, זמנים, pen_color כהקסה, pen_size, id). |
| **access.py** | דיאלוג ניהול הרשאות: שורה לכל משתמש עם רשימת בחירה לרמת גישה (Admin/Edit/View/No Access). בעת שמירה מחזיר מילון username → access string כדי שהורה יוכל לשלוח פקודות `change_access` מתאימות. |
| **flow_layout.py** | פריסת זרימה (`QLayout`) עבור וידג'טים: ממקם וידג'טים משמאל לימין ועובר שורה כשאין מקום; משמש לכפתורי המחברות ב־`MainWindow`. תומך בניקוי והחלפת אוסף הווידג'טים. |
| **protocol.py** | פרוטוקול זהה בצד הלקוח: length‑prefixed send/recv עבור מחרוזות ובתים, עם הצפנת AES אופציונלית כאשר קיים מפתח בחיבור. |
| **key_exchange.py** | החלפת מפתחות בצד הלקוח: `send_recv_key` שולחת מפתח ציבורי של הלקוח, מקבלת מפתח שרת וגוזרת מפתח משותף (מראה את זרימת `recv_send_key` בצד השרת). |
| **aes_cipher.py** | זהה לשרת: AES במצב CBC להצפנה/פענוח ויצירת מפתח עבור הערוץ המאובטח. |
| **diffie_hellman.py** | זהה לשרת: ECDH על SECP384R1 ו־HKDF להפקת מפתח משותף. |
| **my_sha256.py** | זהה לשרת: כלי גיבוב SHA‑256 (בתים והקסה); בצד הלקוח משמש בעיקר לבדיקה מקומית, בעוד שהשרת משתמש בגיבוב לסיסמאות. |
| **constants.py** | קבועים בצד הלקוח: IP ופורט ברירת מחדל, אורך הודעה, אינדקסי פרמטרים בבקשות ומספרי פרמטרים צפויים לפעולות (login, signup, get_notebook, add_notebook, clients_notebooks, add_stroke, delete_stroke וכו'). |
| **style.py** | קבועי UI וסגנונות: גודל חלון, מרווחים, מידות קנבס ורקע, טווחי עט/מדגיש/מחק, צבעים וסגנונות Qt עבור חלון ראשי, סרגלי כלים, כפתורים, רכיבי התחברות, קומבובוקסים, פסי גלילה ודיאלוגי הרשאות. מגדיר גם את `ToolbarsEnum` למיפוי סרגלי הכלים. |
| **winreg_file.py** | קורא IP ופורט של השרת מרישום Windows (`HKEY_LOCAL_MACHINE\SOFTWARE\Technition Server`) לצורך אתחול הלקוח ללא קידוד קשיח של הכתובת בקוד. |
| **pep8_tester.py** | סקריפט עזר להרצת בדיקות סגנון PEP8 על קבצי Python בספרייה הנוכחית באמצעות החבילה `pep8`. |

---

## סיכום

- **שרת:** 11 קבצים (server,‏ מנהלי משתמשים/מחברות/קשרי משתמש–מחברת, פרוטוקול, קריפטוגרפיה, החלפת מפתחות, methods, קבועים, SHA‑256).
- **לקוח:** 21 קבצים (client,‏ חלונות login/signup/main/notebook‑area, מחברת וקנבס, stroke, דיאלוג הרשאות, flow layout, scroll area, פרוטוקול, קריפטוגרפיה, החלפת מפתחות, קבועים, style, רישום, pep8 tester).

סה״כ: **32** קבצי Python.

